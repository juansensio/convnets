train: 
  devices: 1
  enable_checkpointing: True

dataloader:
  num_workers: 10
  pin_memory: True
  
logger:
  project: alexnet
  name: exp003

scheduler: ReduceLROnPlateau
scheduler_params:
  patience: 3
  threshold_mode: rel
  verbose: true